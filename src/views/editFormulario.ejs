<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Editar Orden</title>
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
  <div class="container">
    <header>
      <h1>Editar Orden de Servicio</h1>
    </header>

    <section class="form-section">
      <form action="/ordenes/edit/<%= orden._id %>" method="POST">
        <div class="form-group">
          <label for="empresa">Empresa:</label>
          <input type="text" id="empresa" name="empresa" value="<%= orden.empresa %>" required>
        </div>

        <div class="form-group">
          <label for="responsable">Responsable:</label>
          <input type="text" id="responsable" name="responsable" value="<%= orden.responsable %>" required>
        </div>

        <div class="form-group">
          <label for="problematica">Problemática:</label>
          <textarea id="problematica" name="problematica" required><%= orden.problematica %></textarea>
        </div>

        <!-- Agrega el resto de campos aquí -->
        
        <button type="submit" class="btn-submit">Guardar Cambios</button>
      </form>
    </section>
  </div>
</body>
</html>
